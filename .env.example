# Required — your Discord bot token from the Developer Portal
DISCORD_BOT_TOKEN=

# Optional — your own server ID for instant command sync during development.
# Without this, global commands take a few minutes to propagate after startup.
# DISCORD_GUILD_ID=

# Optional — override the data directory (useful for Railway/Fly.io volume mounts).
# Defaults to ./data relative to the project root.
# DATA_DIR=/data
